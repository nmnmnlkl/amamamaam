# Allow postinstall scripts to run
# This is needed for some packages that require build steps
auto-install-peers=true
# Ensure we install all dependencies, including optional ones
include=dev
# Use the latest npm for better compatibility
engine-strict=true
