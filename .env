# Create a new .env file
echo "# Server Configuration
PORT=3000
HOST=localhost
NODE_ENV=development

# Database connection (you'll need to get this from Neon)
DATABASE_URL=your_neon_database_connection_string_here" > .env